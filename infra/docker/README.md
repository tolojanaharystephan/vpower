# App Dockerfiles — Phase 17 full multi-stage builds.
# Phase 1 uses Compose only for PostgreSQL + Redis.

# Placeholder paths:
# - infra/docker/Dockerfile.api
# - infra/docker/Dockerfile.client
# - infra/docker/Dockerfile.admin
